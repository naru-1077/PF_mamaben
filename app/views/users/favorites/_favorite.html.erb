<% if post.favorited_by?(current_user) %>
  <%= link_to image_tag(src= "favorite-release.png"), post_favorites_path(post), method: :delete, remote: true %>
<% else %>
  <%= link_to image_tag(src= "favorite.png"), post_favorites_path(post), method: :post, remote: true %>
<% end %>