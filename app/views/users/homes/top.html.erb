<div class="container">
  <div class="row">
    <div class="col-md-12">

      <div class="main-visual">
        <div class="main-visual-content">
          <p class="text-white">ようこそ、mama弁へ！</p>
          <p class="text-white">このサイトは、お子様用のお弁当投稿をまとめたサイトになります。</p>
          <p class="text-white">アカウントをお持ちでない方も、投稿の閲覧はできますが、</p>
          <p class="text-white">ご自身で投稿することや、お気に入り登録にはアカウント登録が必要になります。</p>
        </div>
      </div>

      <h3 class="fas fa-utensils">新着投稿</h3>
      <div class="new-posts">
        <% @posts.each do |post| %>
          <table class= "top d-inline-block">
            <tr>
              <td>
                <%= link_to post_path(post), data: {"turbolinks" => false} do %>
                  <%= attachment_image_tag post, :image, format: 'jpeg', size: "365x225" %>
                <% end %>
              </td>
            </tr>
            <tr>
              <td><%= "アカウント名: #{post.user.name}さん" %></td>
            </tr>
            <tr>
              <td><%= "ジャンル: #{post.genre.genre_name}" %></td>
            </tr>
            <tr>
              <td><%= "タイトル: #{post.title}" %></td>
            </tr>
            <tr>
              <td><%= "お気に入り数: #{post.favorites.count}件" %></td>
            </tr>
            <tr>
              <td><%= "コメント数: #{post.post_comments.count}件" %></td>
            </tr>
            <tr>
              <td><%= "投稿日時: #{post.created_at.strftime('%Y/%m/%d')}" %></td>
            </tr>
          </table>
        <% end %>
      </div>

        <h3 class="fas fa-utensils">お気に入り数ランキング</h3>
        <div class="ranking-posts">
          <% @all_ranks.each_with_index do |post, idx| %>
            <table class= "top d-inline-block">
              <tr>
                <td class="ranking"><%= "第#{idx + 1}位" %></td>
              </tr>
              <tr>
                <td>
                  <%= link_to post_path(post) do %>
                    <%= attachment_image_tag post, :image, format: 'jpeg', size: "365x225" %>
                  <% end %>
                </td>
              </tr>
              <tr>
                <td><%= "アカウント名: #{post.user.name}さん" %></td>
              </tr>
              <tr>
                <td><%= "ジャンル: #{post.genre.genre_name}" %></td>
              </tr>
              <tr>
                <td><%= "タイトル: #{post.title}" %></td>
              </tr>
              <tr>
                <td><%= "お気に入り数: #{post.favorites.count}件" %></td>
              </tr>
              <tr>
                <td><%= "コメント数: #{post.post_comments.count}件" %></td>
              </tr>
              <tr>
                <td><%= "投稿日時: #{post.created_at.strftime('%Y/%m/%d')}" %></td>
              </tr>
            </table>
          <% end %>
        </div>

        <h5 class="text-md-right">
          <%= link_to "全ての投稿を見る", posts_path, class: "fas fa-utensils", data: {"turbolinks" => false} %>
        </h5>

    </div>
  </div>
</div>